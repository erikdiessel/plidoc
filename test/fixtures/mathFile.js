/* This code includes mathematical formulas set with Mathjax */
var fibonacci = function(n) {
    return math.floor(math.pow((1+math.sqrt(5))/2, n));
}
/* This code is based on the formula:
<div>
$$ a_n = \lfloor \phi^n \rfloor $$
</div>

*/